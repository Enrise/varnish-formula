{% set varnish = salt['grains.filter_by']({
    'Debian': {
        'pkg': 'varnish',
        'service': 'varnish',
        'config_files': {
          'config': '/etc/default/varnish',
          'vcl': '/etc/varnish/default.vcl',
        }
    },
}, merge=salt['pillar.get']('varnish:lookup'), default='Debian') %}
